---
# paths to search for repo files
search_paths:
 - /opt/flight/opt/repoman/lists
 - /opt/flight/var/lib/repoman
# storage for local repo files and client repo configs
repolist_root: '/opt/flight/var/lib/repoman'
# default url base to use for client repo configs
repo_url: 'http://localhost/repos'
# default repo root for mirrors
repo_root: /opt/repos
